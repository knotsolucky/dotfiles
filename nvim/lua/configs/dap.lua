-- DAP: adapters and configs are registered by mason-nvim-dap (codelldb, python, javadbg).
-- Install with :DapInstall codelldb / :DapInstall python / :DapInstall javadbg
-- Keys: <leader>db breakpoint, <leader>dc continue, <leader>di step into, <leader>do step over, <leader>dO step out
require("dap")
