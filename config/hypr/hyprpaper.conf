# Use monitor = , to apply to all outputs.
# Keep the wallpaper file in ~/.config/HyprPaper/ to stay inside dotfiles flow.
wallpaper {
  monitor = ,
  path = ~/.config/HyprPaper/wallpaper.jpg
  fit_mode = cover
}
